# Optional: increases Google PageSpeed Insights quota. Without a key, PSI
# still works but is limited to a small number of requests per day.
# Get one at https://developers.google.com/speed/docs/insights/v5/get-started
GOOGLE_PSI_API_KEY=

# Port the Express API server listens on.
PORT=3001
